# ---- API (apps/api) ----
DATABASE_URL=postgres://postgres@localhost:5432/roster
PORT=3000
CORS_ORIGIN=http://localhost:5173
# How long a seat stays `locked` after booking before the hold lapses and the seat
# becomes claimable again. Lazy expiry — no cron job needed. (NFR13, UC3-A3)
SEAT_HOLD_MINUTES=10

# ---- API tests / load tests ----
TEST_DATABASE_URL=postgres://postgres@localhost:5432/roster_test
LOAD_DATABASE_URL=postgres://postgres@localhost:5432/roster_load

# ---- Web (apps/web) ----
VITE_API_BASE_URL=http://localhost:3000
